<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/index.css">
</head>
<body>
    <div class = "main w-full min-h-screen bg-zinc-900 text-white">
    <div class = "form p-10">
    <form action="/create" method="post">
        <input class = "block w-full rounded-md px-5 py-2 bg-zinc-800" placeholder="Title goes here" name = "title">
        <textarea class = "block w-full mt-2 rounded-md px-5 py-2 bg-zinc-800 resize-none" placeholder="write content" name = "details"></textarea>
        <input class = "mt-2 bg-blue-700 rounded-md p-2" type="submit" >
    </form>
    </div> 
    <div class = "tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length > 0){ %>
            <%files.forEach(function(val){ %>
                <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800 relative">
                    <a href="/delete/<%= val %>">
                        <button type="button" class="absolute top-2 right-2 rounded-md p-2 text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-50">
                            <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                            </svg>
                        </button>
                    </a>
                    <h1 class="text-2xl mt-6"><%= val %></h1>
                    <div class = " mt-3 flex justify-between">
                    <a class="text-blue-600  block" href="/files/<%= val %>">Read More.</a>
                    <a class = "text-zinc-500" href="/edit/<%=val%>">Edit name</a>
                    </div>
                </div>
            <% }) %>
        <%}else{%>
            <h1 class = "text-2xl">Nothing is here.</h1>
           <% } %>

    </div> 
    </div>
</body>
</html>