<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class = "w-full h-full bg-zinc-900 p-10 text-white">
        <div class = "nav mb-8 text-blue-500">
            <a href="/">back</a>
        </div>
        <h1 class = "text-3xl tracking-tighter mb-3">User List</h1>
        <% if(users.length > 0){ %>
        <div class = "users  flex gap-3 flex-wrap">
            <% users.forEach(function(user){%>
            <div class = "user max-w-56 p-4 bg-zinc-800 rounded-lg">
                <div class = "w-full h-72 rounded-lg  bg-zinc-700 overflow-hidden"><img class = "w-full h-full object-cover object-top" src=<%= user.imgurl%> alt=""></div>
                <h3 class = "text-xl tracking-tighter mt-3"><%=user.name%></h3>
                <h5 class = "email text-zinc-500"><%=user.email%></h5>
                <div class = "flex gap-3 mt-10">
                    <a href="/edit/<%=user._id%>" class = "text-zinc-400">Edit User</a>
                    <a href="/delete/<%=user._id%>" class = "text-red-500">Delete User</a>    
                </div>
            </div>
            <% }) %>
        </div>
        <% }else{ %>
            <h1 class = "text-2xl tracking-tighter">No Users</h1>  
        <%}%>  
</body>
</html>